  - type: web
    name: llm-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    plan: starter  # or standard if needed
